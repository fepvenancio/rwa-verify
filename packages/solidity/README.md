# @rwa-verify/solidity (WS3)

`RwaVerify` view library and `CollateralGate` example. Pure/view only, OpenZeppelin v5 only, no Solady, no upgradeability.

```bash
forge test
forge test --profile diff   # differential tests, needs local Anvil with Kula reference impls (ADR-003)
```
