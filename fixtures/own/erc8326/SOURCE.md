# Source

- Origin: generated by this repository (`packages/canon`, WS1) from the text of `specs/erc-8326.md` at the pinned commit (`specs/PINNED.md`).
- License: MIT (this repository). No third-party test vectors are reproduced here.
- Generated: 2026-09-12 with `buildEntry` and `computeCanonicalBundleHash` from `@rwa-verify/canon`.
- Use: regression vectors for the canonicaliser and bundle hash; loaded and re-verified by `packages/canon/src/fixtures.test.ts`.

Each file holds the raw input documents (`documents`, inline as `content` text or `contentHex` bytes), the derived `entries` in input order, and the expected `bundleHash`.

`spec-normative-bundle.json` is the one fixture whose expected hash is independently authoritative: it is the bundle vector published in the ERC itself. The others pin this implementation's behaviour and were checked against the spec's rules by review and the unit tests, not by a second implementation.
