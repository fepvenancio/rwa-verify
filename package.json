{
  "name": "rwa-verify",
  "private": true,
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:sol": "forge test --root packages/solidity",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
